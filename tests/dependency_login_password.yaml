name: "Dependency Password Login"
url: "file:///Users/alieissa/Projects/Coding/vizQA/demo_site/dependency_auth_lab.html"
description: "Uses inherited credentials from the seed test and completes the password step before MFA."

requires:
  - dependency_auth_seed

artifacts:
  username: "analyst.user"
  password: "AnalystPass!23"

steps:
  - action: "Click the 'Sign In' button"
    expect: "A centered Password Sign In modal should appear"
  - action: "Type {username} into the username field"
    expect: "The username field should contain 'analyst.user'"
  - action: "Type {password} into the password field"
    expect: "The password field should be filled"
  - action: "Click the 'Continue to MFA' button"
    expect: "'Complete Multi-factor Authentication' modal"
